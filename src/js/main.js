// import scrollAction from './blocks/scrollAction';
import dropFilterInit from './blocks/dropFilterInit';
import inputRange from './blocks/inputRange';

window.addEventListener('load', (q) => {
	dropFilterInit();
	inputRange();
	// scrollAction();
});
